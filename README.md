
  # Event RSVP System

  This is a code bundle for Event RSVP System. The original project is available at https://www.figma.com/design/axjAYMP7udlLOaU6dgwgzB/Event-RSVP-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  